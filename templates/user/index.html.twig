{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <h1>{{ app.user.username }}</h1>
    <a href="{{ path('app_user_follower', {'username': app.user.username}) }}"><p>Seguidores: {{ app.user.followers|length }}</p></a>
    

    <a href="{{ path('app_post_index') }}">Posts</a>
{% endblock %}
